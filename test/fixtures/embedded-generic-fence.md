---
title: Generic fence schema note
---

# Generic fence schema note

No `dbml` fence tag here, just a plain fence whose body looks like DBML.

```
Table widgets {
  id bigint [pk]
  name varchar(50) [not null]
}
```
